NET=clr
OS=windows

ifeq ($(NET),clr)
  CSC=csc
else ifeq ($(NET),mono)
  #CSC=gmcs
  # Temporary workaround until assembly loading is figured out
  CSC=gmcs /nostdlib /r:mscorlib.dll
endif

ifeq ($(OS),windows)
  PYTHON_DLL=C:/WINDOWS/system32/python25.dll
  OBJ_FORMAT=win32
else ifeq ($(OS),linux)
  PYTHON_DLL=/usr/lib/libpython2.5.so.1
  OBJ_FORMAT=elf32
endif
